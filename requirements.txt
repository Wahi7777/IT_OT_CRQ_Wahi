# Runtime pins for `pip install -r requirements.txt` and `pip-audit -r requirements.txt`.
# Canonical package metadata is pyproject.toml. Keep the numpy/openpyxl versions identical.
# et-xmlfile is the openpyxl transitive dependency, pinned so audits see a full lock.
# V1 application/Lambda runtime: CPython 3.12. Historical freeze runtime is recorded in its immutable fixture metadata.
numpy==2.5.2
openpyxl==3.1.5
et-xmlfile==2.0.0
